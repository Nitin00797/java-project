pipeline {
    agent none
    stages {
        stage("foo") {
            steps {
                echo "Hello World"
            }
        }
    }
    post {
        always {
            echo "Runs Always"
        }
        changed {
            echo "Runs on Change"
        }
        success {
            echo "Successful Build"
        }
    }

}

