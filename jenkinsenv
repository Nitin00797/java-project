pipeline {
    agent {
            dockerfile {
                 filename 'Dockerfile.build'
                 dir 'src'
                 additionalBuildArgs '--build-arg version=1.0.2'
            }

        }

    stages {
        stage("Run cmd") {
            steps {
                    echo "Hello, Dockerfile"
                    python hello.py
                }
            }
    }
}

